function setup() {
  createCanvas(600, 500);
  background("green");//cor do fundo 
}

function draw() {
  
  
  stroke("red");
  
  fill("blue");
  
  if(mouseIsPressed)
    
(rect(mouseX,mouseY,80,80))// cria um retangulo
  
  
  
}
